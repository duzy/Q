'use strict';

describe('Test Quiz.Login', function() {

    beforeEach(module('Quiz.Login'));

    describe('Test Quiz.Login controller', function(){

        it('should ....', inject(function($controller) {
            expect($controller('Quiz.LoginCtrl')).toBeDefined();
        }));
        
    });
});
