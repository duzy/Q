'use strict';

describe('Test Quiz.Results', function() {

    beforeEach(module('Quiz.Results'));

    describe('Test Quiz.Results controller', function(){

        it('should ....', inject(function($controller) {
            expect($controller('Quiz.ResultsCtrl')).toBeDefined();
        }));
        
    });
});
