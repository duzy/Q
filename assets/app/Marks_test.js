'use strict';

describe('Test Quiz.Marks', function() {

    beforeEach(module('Quiz.Marks'));

    describe('Test Quiz.Marks controller', function(){

        it('should ....', inject(function($controller) {
            expect($controller('Quiz.MarksCtrl')).toBeDefined();
        }));
        
    });
});
