'use strict';

describe('Test Quiz.Logout', function() {

    beforeEach(module('Quiz.Logout'));

    describe('Test Quiz.Logout controller', function(){

        it('should ....', inject(function($controller) {
            expect($controller('Quiz.LogoutCtrl')).toBeDefined();
        }));
        
    });
});
