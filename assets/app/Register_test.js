'use strict';

describe('Test Quiz.Register', function() {

    beforeEach(module('Quiz.Register'));

    describe('Test Quiz.Register controller', function(){

        it('should ....', inject(function($controller) {
            expect($controller('Quiz.RegisterCtrl')).toBeDefined();
        }));
        
    });
});
